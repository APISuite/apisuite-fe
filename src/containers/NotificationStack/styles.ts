import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  stack: {
    display: 'flex',
    flexDirection: 'column',
    position: 'fixed',
    top: 42,
    width: '100%',
    zIndex: 1300,
  },
})
