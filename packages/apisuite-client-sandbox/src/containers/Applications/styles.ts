import { makeStyles } from '@material-ui/styles'
import theme from 'theme'

export default makeStyles(({
  root: {
    minHeight: '100%',
    paddingTop: 200,
    backgroundColor: '#fff',
  },
  contentContainer: {
    maxWidth: 900,
    margin: '0 auto',
  },
}))
